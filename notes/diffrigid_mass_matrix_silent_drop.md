# Mass-matrix backward chain silent drop (LDLT factorization)

## TL;DR

The differentiable rigid solver has a major silent-AD chain drop in the
mass-matrix path:

```
Forward:  cinr → mass_mat → mass_mat_L / mass_mat_D_inv → LDLT solve → acc
Backward: acc.grad → force.grad        (works, via Phase B)
          mass_mat.grad → cinr.grad    (NEVER FIRES — silent drop)
```

`func_factor_mass` is gated `if qd.static(not BW):` — it skips the
factorization in backward replay. That gate exists because the LDLT
forward is a cross-iter self-referential update pattern that Quadrants
AD cannot reverse-mode automatically (same family as the Phase B Step 1
issue: trying to remove the gate produces

```
RuntimeError: reverse_segments.cpp:reverse_segments@68] Invalid program
```

at kernel-compile time). So the gate is correct as a workaround for
*Quadrants' limitation*, but it disconnects the `mass_mat.grad` chain
entirely — there is no other code path that propagates
`mass_mat_L.grad → mass_mat.grad`.

## Why this matters

`mass_mat` depends on `cinr_*` and `cdof_*` (link composite inertia +
motion subspace), which in turn depend on `qpos` (and on `vel` through
`cdofvel`). A correct backward chain through the mass matrix would let
`acc.grad` flow into `qpos.grad` / `vel.grad` via the *mass conditioning*
sensitivity, not just via the Coriolis chain in
`fwd_dynamics_without_qacc.grad`.

In multi-step backward, input force at step `t` changes `vel_{t+1}`,
which changes `cdofvel_{t+1}` and hence `mass_mat_{t+1}` slightly. The
correct gradient through this depends on the LDLT reverse path. With the
silent drop, that contribution is missing — every step's analytical
`qpos/vel.grad` has a systematic error whose magnitude depends on how
much the input force shifts the mass matrix.

This matches the empirical signature of the J5/J4 multi-step failures
(`notes/diffrigid_multistep_grad_leak.md`):

* J1 free, J2 revolute, J3 prismatic: bit-exact backward (max abs ≤
  1e-15 across all seeds × N). These all have static mass matrices on
  fp64 — `cinr_*` is constant in time because no link transform changes
  affect them (J1 has only a freejoint with constant COM offset; J2/J3
  have a single link). So the silent drop has nothing to chain through.
* J4 free+revolute: max_rel = 0.65, max_abs = 7e-5 (after fixing
  `qd_quat_to_xyz` + `kernel_COM_links`). The free joint quat drives
  link transforms which drive cinr/cdof — every step has different
  mass conditioning, and the missing chain is dominant.
* J5 chain3: same family; the link transforms vary through forward
  kinematics so the mass matrix moves every step.

The ratio distribution we measured (20 seeds, J5 N=10 step 0):

```
DOF 0: mean=0.81, std=0.77, range=[-2.02, 1.83]
DOF 1: mean=1.01, std=0.12, range=[ 0.66, 1.19]
DOF 2: mean=1.14, std=0.47, range=[ 0.12, 2.49]
```

Median ≈ 1.0 with a fat-tailed distribution is exactly what
input-dependent missing-chain noise looks like: average input keeps the
mass matrix near-identity (small effect), unlucky inputs swing it.

## The fix path

A Phase B-style manual reverse kernel is required:

1. **`mass_mat.grad ← mass_mat_L.grad + mass_mat_D_inv.grad`**: closed
   form. For LDLT `M = L D L^T`, the differential is
   `dM = dL · D · L^T + L · dD · L^T + L · D · dL^T`. Reverse-mode:

   ```
   M.grad = L^-T · (sym(L^-1 · M_L.grad)·D + D_inv·M_D_inv.grad·???) · L^-1
   ```

   (Smith 1995 / Murray 2016 give the precise formulas.)

2. **Where to invoke**: similar to Phase B — a new kernel
   `kernel_factor_mass_reverse_bw` invoked from
   `RigidSolver.substep_pre_coupling_grad` after `kernel_compute_qacc.grad`
   so it seeds `mass_mat.grad` from `mass_mat_L.grad` /
   `mass_mat_D_inv.grad`. Then `kernel_compute_mass_matrix.grad` (already
   auto-generated by Quadrants because its forward chain
   `cinr → mass_mat` is a straightforward polynomial) will pick up
   `mass_mat.grad` and propagate to `cinr.grad`.

3. **Per-DOF split needed**: The forward LDLT update is cross-iter, so
   the closed-form reverse will also be cross-iter on `i_d`. Phase B
   already handled this for Step 1 BW by splitting per-DOF from Python;
   the same trick should apply here.

4. **Verification**: re-run `notes/diag_multistep_worst_case.py` and
   `notes/diag_j5_ratio_dist` (in `/tmp/`). Success criterion: J5/J4
   ratio distribution mean and median both at 1.0 with std < 0.05;
   max_abs / max_rel scale linearly with N (no multi-step amplification).

## Diagnostic trail

Investigation commits:

* `38a34d23` — `acc_smooth_bw.grad` per-substep zeroing → fixed
  J1/J2/J3 multistep.
* `5090aa6a` / `7e504a1c` — `kernel_COM_links` chains the cinr/cdof
  reverse (Coriolis-side); contributes ~5e-10 per step.
* `83a49102` / `ade3a3a7` — branchless / indexing rewrites of
  `qd_quat_to_R` / `qd_quat_to_xyz` /
  `qd_transform_inertia_by_trans_quat` → halved J4 max_rel
  (1.34 → 0.65).

After all four, J5 max_abs is still 7e-6 (N=10) and J4 max_rel still
0.65 — the residual is consistent with this mass-matrix silent drop.

## Open question

The fix is correct in principle but ~2-3 hours of work (writing closed-
form LDLT reverse, per-DOF kernel split, unit test for the reverse in
isolation, integration into `substep_pre_coupling_grad`). Logged here
so the next session can pick it up without re-deriving.
